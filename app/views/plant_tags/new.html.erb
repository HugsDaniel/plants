<h1>Add a tag for <%= @plant.name %></h1>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-12 col-md-8">
      <%= simple_form_for [@plant, @plant_tag] do |f| %>
        <%= f.input :tag, collection: Tag.all, input_html: { class: "select2-multiple-input", multiple: true }, include_hidden: false %>

        <%= f.submit "Créer !" %>
      <% end %>
    </div>
  </div>
</div>
